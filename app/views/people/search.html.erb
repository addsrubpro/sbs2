<!-- Search results page -->
<h1>Listing people - search result</h1> 
<%= "<b>Birthdate</b> is between <i>#{@birthdate_low}</i> and <i>#{@birthdate_high}</i> AND <b>Income classification</b> is <i>#{@ic_description}</i> AND <b>Occupation classification</b> is <i>#{@oc_description}</i>. " %> 

<table>
  <tr>
    <th>Party ID</th>
    <th>Current last name</th>
    <th>Current first name</th>
    <th>Birth date</th>
    <th>Income classification</th>
    <th>Occupation classification</th>
    <th>User account name</th>
  </tr>
  <%= render @people %>     <!-- calls the partial _person.html.erb for each Person object collected in the @people array-->
</table>


<%= link_to 'New person', new_person_path %>  |  
<%= link_to 'Clear search', people_path %>